<%= form_for(@item_line_price) do |f| %>
  <% if @item_line_price.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@item_line_price.errors.count, "error") %> prohibited this item_line_price from being saved:</h2>

      <ul>
      <% @item_line_price.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :item_line_id %><br />
    <%= f.number_field :item_line_id %>
  </div>
  <div class="field">
    <%= f.label :recommended_price %><br />
    <%= f.number_field :recommended_price %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
